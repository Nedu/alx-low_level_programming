#include <stdio.h>
#include "main.h"

/**
 * _strstr - strstr
 * Program that prints its name, followed by a new line.
 * @argc: int argc.
 * @argv: char argv array.
 * Author - Nedu Robert
 * Return: Returns 0.
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
