#include "main.h"
/**
 * add - Adds two integers and returns the result.
 *
 * @a: input a.
 * @b: input b.
 *
 * Return: Returns the result of adding two integers
 */
int add(int a, int b)
{
	return (a + b);
}
