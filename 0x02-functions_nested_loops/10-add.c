#include "main.h"

/**
 * add - a + b
 * Function that adds two integers and returns the result.
 * @a: input number.
 * @b: input number.
 * Author - Nedu Robert
 *
 * Return: Returns sum of the two inputs
 */
int add(int a, int b)
{
	return (a + b);
}
