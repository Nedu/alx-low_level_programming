#include "main.h"

/**
 * _islower - islower
 * Function that checks for lowercase character.
 * Author - Nedu Robert
 *
 * Return: Returns void
 */
int _islower(int c);
{
	if (c >= 97 and c <= 122) {
        return (1);
    }
    return (0);

}

